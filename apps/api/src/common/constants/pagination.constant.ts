export enum PaginationPage {
  MIN = 1,
}

export enum PaginationSize {
  MIN = 1,
  // MAX = 100,
  MAX = 1000,
  // DEFAULT = 10,
  DEFAULT = 500,
}

export enum SortDirection {
  ASC = 'asc',
  DESC = 'desc',
}
